# 10/24일 강의
함수 : 1개
클래스 : N개 Data + N개 함수
모듈 : 1개 파일
패키지 : 폴더
  - win32 API
  - JavaFramework

1) import만 쓰는 경우 simpleset.union(), simpleset.intersect() ==> FM 사용 형식
2) from xxx import 함수명 : 해당 함수명만 로딩  ==> 편하게 쓰기는 좋다
3) from xxx import *
4) import pandas as pd : 별칭

simpleset.py(소스코드) --> simpleset.cpython.pyc(바이트코드)

라이브러리가 뭐가 있는지 알려면 커뮤니티 가입 (파이썬 코리아 등등...)